vplot
-----

A suite of plotting routines for ``vplanet``. It can be used to both generate a
plot of all ``vplanet`` output data, or combined with ``matplotlib`` to easily
import data for professional figures.
